#pragma once
#include <DxLib.h>

/// <summary>
/// ‚æ‚­g‚¤F‚ğ’è‹`
/// </summary>
namespace DxLib {
	static const int DX_WHITE = GetColor(255, 255, 255);
	static const int DX_BLACK = GetColor(0, 0, 0);
	static const int DX_RED = GetColor(255, 0, 0);
};